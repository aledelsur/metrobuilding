<h1>Editar Deuda</h1>

<%= form_with(model: [:admin, @debt], local: true) do |form| %>
  <%= render 'form', debt: @debt, form: form %>

  <div class="actions">
    <%= form.submit 'Actualizar Deuda', class: 'btn btn-info' %>
  </div>
<% end %>

<%= link_to 'Back', admin_debts_path %>
